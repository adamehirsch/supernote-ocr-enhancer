# Supernote OCR Enhancer Dependencies

# Supernote .note file parsing
supernotelib>=0.6.4

# HTTP client for OCR API
requests>=2.31.0

# FastAPI for health endpoint
fastapi>=0.109.0
uvicorn>=0.27.0

# Image handling
pillow>=10.0.0

# Data validation
pydantic>=2.0.0

# SQLCipher database support (for Mac app encrypted database)
# Build requires: brew install sqlcipher
# Install with: C_INCLUDE_PATH=$(brew --prefix sqlcipher)/include LIBRARY_PATH=$(brew --prefix sqlcipher)/lib pip install pysqlcipher3
pysqlcipher3>=1.0.0
